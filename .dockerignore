# Keep the image lean. The dashboard SOURCE is needed (the Docker UI stage
# builds it), but its node_modules / dist / env are not.
.git
**/node_modules
**/dist
automation-hub-dashboard/.env
automation-hub-dashboard/.env.*
**/__pycache__
*.egg-info
**/*.db
.pytest_cache
report.html
